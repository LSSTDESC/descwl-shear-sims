name: shear-meas-tests

on:
  push:
    branches:
      - master
  pull_request: null

jobs:
  shear-meas-tests:
    name: shear-meas-tests
    strategy:
      matrix:
        pyver: [3.8]

    runs-on: "ubuntu-latest"

    steps:
      - uses: actions/checkout@v2

      - uses: conda-incubator/setup-miniconda@v2
        with:
          python-version: ${{ matrix.pyver }}
          channels: conda-forge
          channel-priority: strict
          show-channel-urls: true

      - name: configure conda and install code
        shell: bash -l {0}
        run: |
          conda config --set always_yes yes
          conda install -q mamba

          mamba install -q stackvana=0

          mamba install -q \
            flake8 \
            pytest \
            numpy \
            galsim \
            numba \
            ngmix \
            lsstdesc-weaklensingdeblending \
            fitsio

          pip install --no-deps -e .

          pushd ..
          git clone https://github.com/LSSTDESC/descwl_coadd.git
          pip install --no-deps -e .
          popd

      - name: test
        shell: bash -l {0}
        run: |
          pytest -vvs pytest -vvs shear_meas_tests/test_shear_meas.py
